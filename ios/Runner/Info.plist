<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>

        <!-- Performance -->
        <key>CADisableMinimumFrameDurationOnPhone</key>
        <true/>

        <!-- App identity -->
        <key>CFBundleDevelopmentRegion</key>
        <string>$(DEVELOPMENT_LANGUAGE)</string>

        <key>CFBundleDisplayName</key>
        <string>JohnClassic</string>

        <key>CFBundleExecutable</key>
        <string>$(EXECUTABLE_NAME)</string>

        <key>CFBundleIdentifier</key>
        <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

        <key>CFBundleInfoDictionaryVersion</key>
        <string>6.0</string>

        <key>CFBundleName</key>
        <string>JohnClassic</string>

        <key>CFBundlePackageType</key>
        <string>APPL</string>

        <key>CFBundleShortVersionString</key>
        <string>$(FLUTTER_BUILD_NAME)</string>

        <key>CFBundleSignature</key>
        <string>????</string>

        <key>CFBundleVersion</key>
        <string>$(FLUTTER_BUILD_NUMBER)</string>

        <key>LSRequiresIPhoneOS</key>
        <true/>

        <!-- Input -->
        <key>UIApplicationSupportsIndirectInputEvents</key>
        <true/>

        <!-- UI -->
        <key>UILaunchStoryboardName</key>
        <string>LaunchScreen</string>

        <key>UIMainStoryboardFile</key>
        <string>Main</string>

        <!-- iPhone only -->
        <key>UIDeviceFamily</key>
        <array>
            <integer>1</integer>
        </array>

        <!-- Portrait only -->
        <key>UISupportedInterfaceOrientations</key>
        <array>
            <string>UIInterfaceOrientationPortrait</string>
        </array>

        <!-- 🔐 BIOMETRICS (OBLIGATOIRE) -->
        <key>NSFaceIDUsageDescription</key>
        <string>
            Face ID est utilisé pour sécuriser l’accès à votre compte et à vos transactions.
        </string>

        <key>NSBiometricsUsageDescription</key>
        <string>
            L’authentification biométrique permet de protéger vos données personnelles.
        </string>

        <!-- 📍 LOCATION (UNIQUEMENT EN UTILISATION) -->
        <key>NSLocationWhenInUseUsageDescription</key>
        <string>
            La localisation est utilisée pour afficher les points de vente à proximité.
        </string>

    </dict>
</plist>